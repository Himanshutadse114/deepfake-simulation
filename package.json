{
  "name": "innvikta-deepfake-awareness",
  "version": "0.2.0",
  "private": true,
  "description": "Consent-gated deepfake awareness simulation for security training.",
  "main": "server/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently \"node --watch server/index.js\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client install && npm --prefix client run build",
    "test": "node --test tests/*.test.js",
    "start": "node server/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.1.0",
    "multer": "^1.4.5-lts.2",
    "replicate": "^1.4.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
